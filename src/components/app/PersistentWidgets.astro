---
import AnalyticsCapture from "@/components/AnalyticsCapture.astro";
import ChatWidgetShell from "@/components/app/ChatWidgetShell";
import PrivacyNoticeBanner from "@/components/app/PrivacyNoticeBanner.astro";

interface Props {
  hideChat?: boolean;
}

const { hideChat = false } = Astro.props;
---

<AnalyticsCapture />
<PrivacyNoticeBanner />
{!hideChat && <ChatWidgetShell client:idle />}
